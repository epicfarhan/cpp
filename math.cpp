#include <iostream>
#include <cmath>
int  main(){
  using namespace std;

  double x = 1.2;
  double y = 3.19;
  double z;

  //z = max(x, y); // gives the biggest number 
  //z = min(x, y); // gives the smallest number
  //z = pow(2, 4); // raises to the number of 4 
  //z = sqrt(9); //squre root of 9
  //z = abs(-3); // turns negative to positive
  //z = round(y); // rounds number
  //z = ceil(y); // raises number to highest
  //z = floor(y); // lowers the number
  cout << z;
  
 

  return 0;
}